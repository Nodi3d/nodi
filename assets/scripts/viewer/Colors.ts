import { Color } from 'three';

const ambientColor = new Color(0x808080);
const defaultStandardColor = new Color(0xC0C0C0);
const selectedStandardColor = new Color(0x27DE60);

export {
  ambientColor,
  defaultStandardColor,
  selectedStandardColor
};
